// import { fromJS } from 'immutable';
// import { makeSelectSeasonsPageDomain } from '../selectors';

// const selector = makeSelectSeasonsPageDomain();

describe('makeSelectSeasonsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
