// import { fromJS } from 'immutable';
// import { makeSelectPlayerDomain } from '../selectors';

// const selector = makeSelectPlayerDomain();

describe('makeSelectPlayerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
