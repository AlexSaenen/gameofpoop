// import { fromJS } from 'immutable';
// import { makeSelectSeriesPageDomain } from '../selectors';

// const selector = makeSelectSeriesPageDomain();

describe('makeSelectSeriesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
